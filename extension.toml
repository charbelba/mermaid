id = "mermaid"
name = "Mermaid"
description = "Support for mermaid, the Python linter and formatter"
version = "0.1.0"
schema_version = 1
authors = []
repository = "https://github.com/charbelba/mermaid"


[slash_commands.echo]
description = "echoes the provided input"
requires_argument = true

[slash_commands.pick-one]
description = "pick one of three options"
requires_argument = true